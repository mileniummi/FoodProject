import React from "react";
import styles from "./index.module.css";

const Footer = () => {
  return (
    <div className={styles.container}>
      <div className="container">that's something like footer</div>
    </div>
  );
};

export default Footer;

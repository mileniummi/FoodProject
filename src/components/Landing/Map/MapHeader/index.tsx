import React from "react";
import styles from "./index.module.css";

const MapHeader = () => {
  return (
    <div className={styles.container}>
      <h2 className={styles.title}>Find interesting offers on the map</h2>
    </div>
  );
};

export default MapHeader;
